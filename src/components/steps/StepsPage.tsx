import { StepsCarousel } from './StepsCarousel';

/**
 * Page component for the 5-step emotional regulation process.
 * Simple wrapper that renders the carousel component.
 */
export function StepsPage() {
  return <StepsCarousel />;
}
